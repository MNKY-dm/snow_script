let selects = document.querySelectorAll('.vsplit > #element');
let input = document.querySelectorAll(' #input .short_description');
let title = '';
console.log("ok ma poule");
selects.forEach(select => {
    console.log(select.value + '\n')
})
